
<wxs module="filter" src="./detail-dynamic.wxs" />
<view class="container content-size">
	<view
	 class="square-list"
	 wx:key="id"
	>
		<view class="dynamic-item">
			<!-- 个人信息+时间 -->
			<view class="perInfo">
				<view class="info">
					<image
					 class="release-avatar"
					 src="{{detailDyna.userInfo[0].avatar || '../../images/defaule-avatar.png'}}"
					 mode="cover"
					></image>
					<view class="">
						<view class="release-name">{{detailDyna.userInfo[0].nickName||'默认昵称'}}</view>
						<view class="release-date">{{filter.formatTime(detailDyna.create_time,'M月D日h:m')}}</view>
					</view>
				</view>
				<view>
				<image bindtap="goDislike" wx:if="{{detailDyna.isLike}}" data-id="{{detailDyna._id}}"
					class="send" src="../../images/collect.png"
					data-authId="{{detailDyna.userId}}"
				></image>
				<image bindtap="gotoLike" data-id="{{detailDyna._id}}"
					data-authId="{{detailDyna.userId}}"
					class="send" src="../../images/no-collect.png" wx:else></image>
				</view>
			</view>
			<!-- 内容 -->
			<view>
				<!-- 文字 -->
				<view class="dynamic-copywriting">
					{{detailDyna.content}}
				</view>
				<!-- 图片 -->
				<view wx:if="{{detailDyna.imgs.length != 0}}" class="content-img" >
					<image src="{{item}}" wx:for="{{detailDyna.imgs}}" wx:key="id" class="radius"></image>
				</view>
			</view>
		</view>
	</view>
</view>