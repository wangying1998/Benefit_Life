
<view>
    <!-- 数据返回的层级太深，可以修改简单一点，因为是详情只有一个 -->
    <image src="../../images/ganmao.png" class="det-img"></image>
    <rich-text nodes="{{diseaseDetail.data[0].desc}}" class="text-desc"></rich-text>
     <view class="title">形成原因？</view>
     <rich-text nodes="{{diseaseDetail.data[0].reason}}" class="text-desc"></rich-text>
     <view class="title">何时就医？</view>
     <rich-text nodes="{{diseaseDetail.data[0].doctor}}" class="text-desc"></rich-text>
    <view class="title">生活建议？</view>
    <rich-text nodes="{{diseaseDetail.data[0].suggest}}" class="text-desc"></rich-text>
</view>
